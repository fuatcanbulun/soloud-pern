import React from "react";
import { LoginLayoutContainer } from "./style";

const LoginLayout = ({ children }) => {
  return <LoginLayoutContainer>{children}</LoginLayoutContainer>;
};

export default LoginLayout;
